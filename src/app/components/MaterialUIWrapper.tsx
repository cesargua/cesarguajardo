"use client"; // ✅ Forces the file to run on the client side

import { ThemeProvider, Button, IconButton, IconButtonProps } from "@material-tailwind/react";

export { ThemeProvider, Button, IconButton};
